<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../../../public/css-1/css-pages.css">
    <title>Foods</title>
</head>
<body>
    <a id="createNew" href="/food/new">Create New Food</a>
    <% for( let i = 0; i < food.length; i++ ) { %>
        <% let a = food[i]; %> 
        <% a = a.toJSON() %>
        <p>id: <%= a.id %></p>
        <p>description: <%= a.description %></p>
        <p>ingredients: <%= a.ingredients %></p>
        <p>meal: <%= a.meal %></p>
        <p>foodType: <%= a.foodType %></p>
        <p>Price: <%= a.price %></p>
        <p>likes: <%= a.likes %></p>
        <a href="food/<%=a.id%>">View Food</a>
        <hr>
    <% } %>
</body>
</html>